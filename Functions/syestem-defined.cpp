#include<iostream>
#include<math.h>
#include<string>

using namespace std;

int main(){
    string s;

    cout << "Enter Any string : ";
    getline(cin, s);

    cout << sizeof(double) << endl;
    cout << s.size();

    // int n, p;

    // cout << "Enter N And P : ";
    // cin >> n >> p;

    // cout << pow(n, p);

    return 0;
}
